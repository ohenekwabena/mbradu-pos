import { ScreenPlaceholder } from "@/components/shell/screen-placeholder";

export default function SalesPage() {
  return (
    <ScreenPlaceholder icon="sales" title="Sales">
      The completed-sales list, filters, and PDF report arrive in a later ticket.
    </ScreenPlaceholder>
  );
}
